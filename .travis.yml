language: python
dist: xenial
sudo: true

matrix:
  include:
    - python: 3.9-dev
      env: TOXENV=py39

install:
  - pip install tox
  - pip install codecov

script:
  - tox

after_success:
  - codecov
